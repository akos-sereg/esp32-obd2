#ifndef __bluetooth_h_included__
#define __bluetooth_h_included__

// in case we are testing the display and the buttons only, it makes sense
// to disable bluetooth. by doing this, bluetooth related shit wont be compiled,
// so uploading binaries to esp32 device will be much quicker
#define CONNECT_BLUETOOTH	0

extern void init_bluetooth(void);

#endif